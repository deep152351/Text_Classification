# Text_Classification
Text Classification using TF-IDF and Machine Learning on IMDb movie reviews. This project builds a sentiment analysis pipeline including text preprocessing, vectorization, and model training. Due to computational constraints, the model was trained on 200 samples instead of the full 50k dataset, resulting in lower accuracy.
